#include <stdio.h>
#include <iostream>
using namespace std;
/*
int main() {
    int a,b,c;
	int arr[3][2]={0};
	//int arr[6] ={0};
	//int fee[100];
    int sum=0;
    cin >> a >> b >> c;
	
	for(int i =0; i<3; i++) {
		cin >> arr[i][0] >> arr[i][1];
	}
    for(int j=1; j<=100; j++){
		int count = 0;
        for(int i=0; i<3; i++){
			if( (arr[i][0]<j) && (arr[i][1]>=j) ){
				//i=0, 1,6
				count++;
			}
        }
		if( count==3 ){
            sum += count*c;
		} else if( count==2 ){
            sum += count*b;
        } else if( count==1 ){
            sum += count*a;
        }
    }

    cout << sum << endl;
}
*/